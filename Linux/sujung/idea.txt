실행하고 싶은 명령어랑 단축키를 파일로 저장해놓는다
예를들어, cat proc/kmsg ( see kernel message )

 -> cat proc/kmsg
가장 많이 실행한 명령어를 카운트 해준당

1. alias 개인설정하기
2. 가장 많이 실행한 명령어를 카운트 해준다
3. 회사에서 로그같은거 파일을 많이보는데,
로그파일이 이미 있을떄, 제가 추출하고 싶은 글자를 입력하면
그 글자가 들어간 라인을 모아서 파일로
grep -n '추출\|글자' ./*

